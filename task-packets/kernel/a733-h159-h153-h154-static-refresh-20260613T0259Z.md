# A733 H159 H153/H154 static validation refresh

Captured: 2026-06-13T02:59Z

Model/tooling constraint: continued work used Codex Desktop / hosted ChatGPT only. No local model and no OpenRouter model was used.

## Purpose

Refresh static evidence for the current H153 evidence-preserving candidate and the H154 no-`mbus-msi-lite0` narrowing candidate before any approval-gated hardware proof.

This note is documentation only. It does not approve hardware runs, Cubie staging, `/boot` writes, kernel commits, patch publication, service changes, or model-routing changes.

## Package re-verification

Host: `strix`

Command:

```sh
out="$(readlink -f /srv/projects/kernel-work/outgoing/a733-h154-no-mbus-msi-lite0-latest)"
cd "$out"
sha256sum -c manifest.txt.sha256
sha256sum -c <(tail -6 manifest.txt | sed "s#  $out/#  #")
```

Result:

- latest symlink resolves to `/srv/projects/kernel-work/outgoing/a733-h154-no-mbus-msi-lite0-d9aa2e15caae-20260613T024821Z`
- `manifest.txt`: OK
- `Image`: OK
- `sun60i-a733-cubie-a7s.dtb`: OK
- `config`: OK
- `build.log`: OK
- `source.diff`: OK
- `source-status.txt`: OK

## Worktree status refresh

H153 worktree:

- Path: `/srv/projects/kernel-work/runtime/a733-ccu-nsi-split-draft`
- Branch: `codex/a733-ccu-nsi-split-draft`
- HEAD: `d9aa2e15caae5085b51a28529fc0c35d189df543`
- Status: ` M drivers/clk/sunxi-ng/ccu-sun60i-a733.c`

H154 worktree:

- Path: `/srv/projects/kernel-work/runtime/a733-ccu-nsi-no-mbus-msi-lite0-draft`
- Branch: `codex/a733-ccu-nsi-no-mbus-msi-lite0-draft`
- HEAD: `d9aa2e15caae5085b51a28529fc0c35d189df543`
- Status: `M  drivers/clk/sunxi-ng/ccu-sun60i-a733.c`

The status difference is intentional: H153 source diff is unstaged; H154 source diff is staged so the H155 package `source.diff` could be generated from `git diff --cached`.

## Static validation refreshed

H153 command summary:

```sh
cd /srv/projects/kernel-work/runtime/a733-ccu-nsi-split-draft
git diff --check
git diff HEAD -- drivers/clk/sunxi-ng/ccu-sun60i-a733.c > /tmp/a733-h153-refresh.diff
scripts/checkpatch.pl --strict /tmp/a733-h153-refresh.diff
make O=/srv/projects/kernel-work/build/a733-ccu-nsi-split-draft \
  ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
  -j$(nproc) drivers/clk/sunxi-ng/ccu-sun60i-a733.o
```

H153 result:

- `git diff --check`: pass
- `checkpatch --strict`: pass (`0 errors, 0 warnings, 0 checks, 56 lines checked`)
- focused arm64 object build: pass

H154 command summary:

```sh
cd /srv/projects/kernel-work/runtime/a733-ccu-nsi-no-mbus-msi-lite0-draft
git diff --cached --check
git diff --cached -- drivers/clk/sunxi-ng/ccu-sun60i-a733.c > /tmp/a733-h154-refresh.diff
scripts/checkpatch.pl --strict /tmp/a733-h154-refresh.diff
make O=/srv/projects/kernel-work/build/a733-ccu-nsi-no-mbus-msi-lite0-draft \
  ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
  -j$(nproc) drivers/clk/sunxi-ng/ccu-sun60i-a733.o
```

H154 result:

- `git diff --cached --check`: pass
- `checkpatch --strict`: pass (`0 errors, 0 warnings, 0 checks, 53 lines checked`)
- focused arm64 object build: pass

## Current decision state

- H153 remains the default evidence-preserving candidate because it matches the known-good hardware diagnostic bundle.
- H154 remains the narrower candidate because it removes only `mbus-msi-lite0`.
- Static validation is clean for both.
- Only hardware proof can decide whether H154 is safe to promote over H153.

## Next gated action

Use the H157 approval packet if the operator approves hardware proof:

- `task-packets/kernel/approvals/a733-h154-no-mbus-msi-lite0-cubie3-proof-approval-20260613T025505Z.md`

## Guardrails

- Do not use Cubie1.
- Do not run H154 hardware proof without explicit operator approval.
- Do not stage to Cubie3 or write `/boot/cthu` without explicit operator approval.
- Do not commit H154 kernel source before hardware result.
- Do not publish or send patches from this note.
