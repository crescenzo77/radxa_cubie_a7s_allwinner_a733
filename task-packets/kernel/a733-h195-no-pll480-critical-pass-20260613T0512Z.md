# A733 H195: no-PLL480-critical narrowing pass

Date: 2026-06-13T05:12:00Z

## Summary

H195 tested whether `pll-periph0-480M` is required after the H194 no-IOMMU-critical pass.

Result: H195 passed.

This means the following extra clocks from H193 are not required for the current direct Cubie3 SDMMC0/root-shell proof:

- `ahb-iommu0-sys`
- `ahb-iommu1-sys`
- `pll-periph0-480M`

The remaining extra H193 critical clocks still needing narrowing are:

- `gic`
- `cpu-peri`

## Candidate

Worktree:

```text
/srv/projects/kernel-work/final/a733-ccu-nsi-v2-no-iommu-no-pll480-critical
```

Branch:

```text
codex/a733-ccu-nsi-v2-no-iommu-no-pll480-critical
```

Head:

```text
0c8107dcd1976f4f143a158023926759b96435cd
```

Commit:

```text
0c8107dcd197 clk: sunxi-ng: a733: drop PLL periph0 480M from critical trial
```

Validation:

- `git diff --check HEAD~1..HEAD`: pass
- `scripts/checkpatch.pl --strict <(git format-patch --stdout -1 HEAD)`: pass, 0 errors, 0 warnings
- full arm64 `Image dtbs` build: pass
- package manifest verification: pass

## Package

Artifact:

```text
/srv/projects/kernel-work/outgoing/a733-h195-no-pll480-critical-0c8107dcd197-20260613T050514Z
```

Hashes:

```text
ebefd3ec87ae3cb417c3850d862edb09d5070c0ce9567b71382e65239abe56b4  Image
6edbb3790de674f7011c8accd0e02d94ea5bcafa11dc127238c8a54da71c622a  sun60i-a733-cubie-a7s.dtb
dda33f2fac329a3e79d633fe200497a5d4c599a2338de3caa10ef8cd3e634202  config
bc93509fa1c8c3f16b410e8e21d6c7592fe1e18f68440a787692fbe7d22ba51e  manifest.txt
```

The staged Cubie3 `/boot/cthu` Image, DTB, config, and manifest hashes matched the package.

## Hardware proof

Direct U-Boot capture:

```text
/srv/projects/cubie-uart/logs/20260613T050720Z-a733-h195-no-pll480-critical-ttyUSB0.uart.log
```

Local copy:

```text
/Users/enzo/projects/homelab/tools/hardware-logs/cubie-uart/20260613T050720Z-a733-h195-no-pll480-critical-ttyUSB0.uart.log
```

Hashes:

```text
544aad84e470b6fce2fb68e01591a25e5558a90117c90f5f6bf6c394660f290d  20260613T050720Z-a733-h195-no-pll480-critical-ttyUSB0.uart.log
9f183ee3bcfe032c503a33048eaa23cf467f6aae0c2e2863ea91746ebe386433  20260613T050720Z-a733-h195-no-pll480-critical-ttyUSB0.uart.log.json
```

Pass markers:

```text
Linux version 7.1.0-rc5-00183-g0c8107dcd197
clk: Disabling unused clocks
PM: genpd: Disabling unused power domains
sunxi-mmc 4020000.mmc: initialized, max. request size: 2048 KB, uses new timings mode
Waiting for root device PARTUUID=db375e07-7682-4d4e-b8bc-a923dd0b027e...
mmc0: new high speed SDXC card at address 544c
mmcblk0: mmc0:544c USD00 117 GiB
mmcblk0: p1 p2 p3
EXT4-fs (mmcblk0p3): mounted filesystem ... ro without journal
VFS: Mounted root (ext4 filesystem) readonly on device 179:3.
Run /bin/sh as init process
/bin/sh: 0: can't access tty; job control turned off
#
```

No `Kernel panic` or `Bad Linux ARM64 Image magic` marker was present.

Cubie3 was power-cycled after the proof and verified back on the vendor kernel:

```text
cubie-3
5.15.147-21-a733
```

## Decision impact

H195 narrows H194. The `pll-periph0-480M` critical flag is not required for the current SDMMC0/root-shell proof.

The current best hardware-proven candidate is H195.

## Next recommended kernel action

Use H195 as the known-good parent for the next narrowing pass.

Next candidate should test the remaining extra criticals:

- remove `gic` and `cpu-peri` critical flags together, or
- remove one of them first if we want the smallest possible attribution step.

