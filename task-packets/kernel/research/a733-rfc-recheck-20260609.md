# A733 RFC Recheck

Date: 2026-06-09

## Scope

Refresh the public A733 CCU/PRCM and pinctrl overlap check before any
maintainer-facing A733 patch preparation.

This packet updates the 2026-06-06 recheck. It is research input only; it is not
runtime proof and does not authorize patch submission.

## Sources Checked

- Refreshed local public-inbox cache:
  `/tmp/lore-linux-sunxi-0.git`
- Remote configured for that cache:
  `https://lore.kernel.org/linux-sunxi/0`
- Fetch result on 2026-06-09:
  `git --git-dir=/tmp/lore-linux-sunxi-0.git fetch --all --prune`
- Cache HEAD after fetch:
  `fd511ce40d821408d48bb149c4499128f2317675`
- Cache HEAD message date after fetch:
  `2026-06-05T19:24:29Z`

Note: the refreshed cache did not expose messages newer than 2026-06-05, so this
is a fresh recheck of the currently available local public-inbox mirror, not a
claim that every kernel list has newer-than-cache coverage.

## Searches Run

```sh
git --git-dir=/tmp/lore-linux-sunxi-0.git log \
  --since='2026-06-06' --all --format='%h%x09%ad%x09%s' \
  --date=short --grep='A733'

git --git-dir=/tmp/lore-linux-sunxi-0.git log \
  --since='2026-06-06' --all --format='%h%x09%ad%x09%s' \
  --date=short --grep='sun60i-a733'

git --git-dir=/tmp/lore-linux-sunxi-0.git log \
  --all --format='%h%x09%ad%x09%an%x09%s' --date=short \
  --grep='A733' -i
```

The `--since='2026-06-06'` A733 and `sun60i-a733` searches returned no hits in
the refreshed cache.

## Public Evidence Still Found

Junhui Liu's A733 CCU/PRCM RFC remains the main public Linux CCU overlap found
in this pass:

```text
a68c56676  2026-03-10  Junhui Liu
[PATCH RFC 0/8] clk: sunxi-ng: Add support for Allwinner A733 CCU and PRCM
```

The latest A733 CCU/PRCM follow-up found in this cache is still:

```text
7064e0737  2026-05-14  Junhui Liu
Re: [PATCH RFC 8/8] clk: sunxi-ng: a733: Add reset lines
```

Andre Przywara's A733 pinctrl RFC remains the main public Linux pinctrl overlap
found in this pass:

```text
5f2058d77  2025-08-21  Andre Przywara
[RFC PATCH 0/9] pinctrl: sunxi: Allwinner A733 support
```

The latest Linux pinctrl follow-up found in this cache remains:

```text
94ad0f743  2025-08-24  Julian Calaby
Re: [RFC PATCH 8/9] pinctrl: sunxi: a523-r: add a733-r compatible string
```

Yixun Lan's A733 pinctrl hits in the same cache remain U-Boot-facing work, not a
Linux pinctrl replacement.

## Workflow Impact

- Keep independent A733 CCU/PRCM work out of maintainer-facing local patches.
- Keep independent A733 pinctrl work out of maintainer-facing local patches.
- Keep the expected sendable direction as a narrow SoC DTSI plus Radxa Cubie
  A7S board DTS slice stacked on the active or accepted CCU/PRCM and pinctrl
  prerequisites.
- Preserve the existing `Depends-on:` references unless a later recheck finds a
  newer accepted or superseding prerequisite:
  - `20260310-a733-clk-v1-0-36b4e9b24457@pigmoral.tech`
  - `20250821004232.8134-1-andre.przywara@arm.com`
- Do not expand the first slice into Ethernet/GMAC, VPU, display, Wi-Fi,
  Bluetooth, USB-C, PCIe, or other unproven hardware blocks.

## Next Action

The runtime proof gate still comes first:

```sh
cd /Users/enzo/projects/homelab && scripts/cubie-interactive-root-install-session --confirm-target-ip 192.168.50.95
```

After corrected-root runtime proof passes, refresh this overlap check again if
the source cache or public list state has advanced before reshaping the patch
series.
