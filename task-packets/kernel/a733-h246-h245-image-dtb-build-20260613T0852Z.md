# A733-SDMMC-H246: H245 Image/DTB Build

Captured: 2026-06-13T08:52:00Z

## Purpose

Build full boot artifacts for the H245 common update-bit v2-series option:
`Image` plus the Radxa Cubie A7S DTB. H245 already had apply, checkpatch,
focused GCC, sparse, and public-hygiene coverage; this refresh checks full
boot-artifact buildability.

This packet is documentation only. It is not a hardware proof, not a resend
approval, not a new public thread, and not a service, cron, mail-routing, or
model-routing change.

## Inputs

- Series artifact:
  `task-packets/kernel/a733-h245-common-update-bit-v2-series-option/`
- Base: `d9aa2e15caae`
- Recorded H201/H200 config SHA256:
  `dda33f2fac329a3e79d633fe200497a5d4c599a2338de3caa10ef8cd3e634202`
- Target DTB: `allwinner/sun60i-a733-cubie-a7s.dtb`

## Method

Created a temporary detached build-host worktree from base `d9aa2e15caae`,
applied H245 patches 1-4 with `git am --3way`, copied the recorded H201/H200
build config, ran `olddefconfig`, then built:

```sh
make -j$(nproc) Image allwinner/sun60i-a733-cubie-a7s.dtb
```

No artifacts were staged to any board. The temporary worktree was removed after
the check. The exact H200 source tree remained clean.

## Applied Series

Temporary applied head:

```text
217d23044c1bcf2da1b4ded20c69656bf25d4181
```

Applied commit order:

```text
ce0b2727f01a clk: sunxi-ng: a733: keep the CPUS AHB bridge clock critical
fe2e13a8d5a8 clk: sunxi-ng: a733: keep storage and NSI fabric clocks critical
a24275ec49ec clk: sunxi-ng: commit update-bit clocks during registration
217d23044c1b clk: sunxi-ng: a733: keep GIC and CPU peri clocks critical
```

Apply and source hygiene:

```text
git am --3way: PASS
git diff --check d9aa2e15caae..HEAD: PASS
```

## Result

Build result:

```text
Image/DTB build return code: 0
warning/error grep: no matching lines
```

Generated artifact hashes:

```text
f1d4cf43de50353d0bf3a1941817492f12e37b9b0716c840e36e3fe263c7b38d  arch/arm64/boot/Image
6edbb3790de674f7011c8accd0e02d94ea5bcafa11dc127238c8a54da71c622a  arch/arm64/boot/dts/allwinner/sun60i-a733-cubie-a7s.dtb
dda33f2fac329a3e79d633fe200497a5d4c599a2338de3caa10ef8cd3e634202  .config
```

Artifact sizes:

```text
arch/arm64/boot/Image                                      43244032 bytes
arch/arm64/boot/dts/allwinner/sun60i-a733-cubie-a7s.dtb       4705 bytes
```

The H245 rebuilt DTB matches the H201 exact hardware-proof DTB hash:

```text
6edbb3790de674f7011c8accd0e02d94ea5bcafa11dc127238c8a54da71c622a
```

## Interpretation

The true H245 common update-bit v2-series option builds full arm64 boot
artifacts cleanly from the recorded H201/H200 configuration. Its Cubie A7S DTB
is byte-identical to the H201 hardware-proof DTB.

This strengthens H245 as a maintainer-directed fallback if reviewers ask for
common `CCU_FEATURE_UPDATE_BIT` handling. It still does not replace H215 by
default, because H215 is the currently submitted exact-hardware-proven narrow
A733 expression, while H245 has broader common semantics.

## Next Action

Keep H215 as the current submitted posture. If maintainer feedback asks for
common update-bit handling, H245/H246 provide the starting point for a real v2.
A real v2 still needs A733 hardware proof before any resend or new submission.
