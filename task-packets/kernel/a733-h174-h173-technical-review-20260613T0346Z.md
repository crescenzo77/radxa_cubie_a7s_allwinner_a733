# A733 H174 H173 technical review

Captured: 2026-06-13T03:46Z

## Purpose

Review the current A733 CCU RFC feedback draft against the latest evidence and
remove stale pre-send wording after the successful b4/lore recipient recheck.

This note is documentation only. It does not approve sending mail, publishing
patches, kernel commits, hardware runs, service changes, cron changes, or
model-routing changes.

## Current draft

- `task-packets/kernel/a733-h173-ccu-rfc-feedback-draft-v7-20260613T0345Z.txt`

H173 supersedes H167 for pre-send wording only:

- H167 said the recipients should still be checked with b4/lore.
- H172 completed the b4/lore check.
- H173 updates the warning to state that Patchew and b4/lore checks have been
  done and that no mail has been sent.

The technical body is unchanged from H167.

## Checks

- Diff against H167 confirms only the draft warning block changed.
- Raw email parser sees:
  - `Subject`: present
  - `In-Reply-To`: present
  - `References`: present
  - `To`: present
  - `Cc`: present
- Public hygiene gate on an isolated directory containing H173:
  - status: PASS
  - matches: 0
- Focused grep for private/local/model-routing terms:
  - no matches.

## Technical consistency review

H173 remains consistent with:

- H151: NSI update bit root-cause verification.
- H158: `mbus-msi-lite0` is part of the verified bundle but not individually
  isolated.
- H170: commit-message draft and upstream wording.
- H171: update-bit implementation review.
- H172: b4/lore thread and recipient recheck.

Specific wording checks:

- `ahb-cpus` is described as an apparent CPU path to R-CCU/RTC register
  windows, not as an overbroad certainty.
- `mbus-msi-lite0` is described only as part of the verified bundle.
- NSI update is described as necessary but not sufficient.
- The MP update-bit nuance is preserved: parent/gate/div helpers can pulse the
  update bit, while the MP rate path itself does not appear to OR it in.
- The final ask is maintainer guidance, not a claim that the local shape must
  be accepted as-is.

## Remaining gates

- Human technical review.
- Explicit operator approval before sending.
- Decision whether to send before or after optional H154 hardware proof.

## Guardrails

- Do not send H173 without explicit operator approval.
- Do not treat H173 as approval for H154 hardware proof.
- Do not create kernel-source commits or generated patch series without
  explicit operator approval.
