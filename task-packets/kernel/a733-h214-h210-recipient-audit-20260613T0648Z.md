# A733-SDMMC-H214: H210 Recipient Audit

Captured: 2026-06-13T06:48:00Z

## Purpose

Audit the recipient set for the H210 normalized RFC/RFT candidate using
kernel-native `scripts/get_maintainer.pl`, then verify a corrected
`git send-email --dry-run` command shape.

This is a validation record only. No mail was sent.

## Candidate

- Candidate: H210 normalized RFC/RFT send candidate
- Source patches checked: `0001` through `0004`
- Cover letter: `0000-cover-letter.patch`

## get_maintainer Result

`scripts/get_maintainer.pl` returned these unique recipients/lists for the
four H210 source patches:

```text
Brian Masney <bmasney@redhat.com>
Chen-Yu Tsai <wens@kernel.org>
Jernej Skrabec <jernej.skrabec@gmail.com>
Michael Turquette <mturquette@baylibre.com>
Samuel Holland <samuel@sholland.org>
Stephen Boyd <sboyd@kernel.org>
linux-arm-kernel@lists.infradead.org
linux-clk@vger.kernel.org
linux-kernel@vger.kernel.org
linux-sunxi@lists.linux.dev
```

## Audit Finding

The H210 dry-run command already included the clock and sunxi maintainers/lists
from `get_maintainer.pl`, plus the broader in-flight A733 CCU RFC thread
recipients.

One `get_maintainer.pl` person was missing from the previous command:

```text
Brian Masney <bmasney@redhat.com>
```

The corrected command shape adds Brian Masney while keeping the existing
RFC-thread recipients and list coverage.

## Corrected Dry Run

The corrected `git send-email --dry-run` command, with Brian Masney added as a
`--to` recipient, returned:

```text
sendemail_dryrun_with_brian=PASS
Result: OK
Result: OK
Result: OK
Result: OK
Result: OK
```

The dry-run log showed Brian Masney on all five messages: cover plus patches
1 through 4.

## Decision Impact

H210 remains not sent. If a later reviewed send decision is made, use the H210
candidate with the H214-adjusted recipient command shape so the RFC/RFT series
includes all `get_maintainer.pl` recipients as well as the existing A733 CCU
RFC thread recipients.
