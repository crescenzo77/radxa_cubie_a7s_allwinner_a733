# Baseline Inventory and Freeze Point — 2026-05-17

## Purpose

This document captures the Slice 0 baseline before the homelab architecture
transition begins implementation.

It is a documentation-only freeze point compiled from existing repo docs and
safe read-only repo/Git checks. It is not a live service validation report.

## Sources Used

- `CURRENT_SLICE.md`
- `ROADMAP.md`
- `HOMELAB_LAYOUT.md`
- `WORKFLOW.md`
- `DECISIONS.md`
- `ROUTING_INVENTORY.md`
- `AGENT_STATUS.md`
- `inventory/models/llm-runtime-topology.md`
- `git status --short`
- `rg --files`

No service commands, `sudo`, Docker commands, systemd commands, model API calls,
network calls, or edits outside this repo were used.

## Freeze Point Boundaries

The current transition baseline is frozen as documentation only.

Do not change during Slice 0:

- Live service files outside this repo.
- OpenCode settings.
- `model-dispatch` runtime files.
- Open WebUI, LiteLLM, Docker, systemd, reverse proxy, SearXNG, monitoring,
  backup, or MCP settings.
- Repo locations.
- `tools/`.

## Host Roles and Known Addresses

| Host | LAN | Tailscale | Current documented role | Target transition role |
|---|---:|---:|---|---|
| `framework` | unknown | yes | Thin client for browser, SSH, VS Code Remote-SSH, optional Continue.dev | Thin client for browser, SSH, editor, and review |
| `thinkcentre` | `192.168.50.225` | `100.127.113.105` | Services hub: Open WebUI, `model-dispatch`, SearXNG, AdGuard, dashboard, proxies; LiteLLM retained for rollback/history | Control plane: Open WebUI, `model-dispatch`, SearXNG, reverse proxy, monitoring, service catalog, tier-1 git mirrors |
| `strix` | `192.168.50.11` | `100.105.138.41` | Target canonical project/source host; reasoning/testbed inference | Canonical source, development, code-graph, and reasoning host |
| `amd` | `192.168.50.252` | `100.107.201.16` | Current coding execution host; OpenCode installed; primary RTX 3090 coder model and RX 7900 XT backup | Mode-switched GPU compute worker for coding, LoRA/training, and creative workloads |
| `minipc` | `192.168.50.76` | yes | LAN backup target | LAN backup and artifact storage |
| `mac mini` | `192.168.50.164` | yes | iMessage relay backend; future tier-2 git mirror | Apple/iMessage services and tier-2 git mirror |
| `oracle` | cloud | yes | Off-site backup, Headscale host | Headscale, off-site backup, and external anchor |
| `cubies` | unknown | unknown | Camera-node hardware setup documented but not active | Edge camera nodes only |

## Open WebUI Route

Documented browser route:

```text
http://192.168.50.225:3000
```

Documented current Open WebUI model API target:

```text
OPENAI_API_BASE_URLS: http://192.168.50.225:4010/v1
```

Current documented path:

```text
Open WebUI -> model-dispatch on ThinkCentre -> local endpoints or explicit OpenRouter-free choices
```

Open WebUI is documented as no longer actively routing through LiteLLM.

## `model-dispatch` Baseline

Documented service details:

| Field | Value |
|---|---|
| Host | `thinkcentre` |
| Service | `model-dispatch.service` |
| Path | `/srv/model-dispatch` |
| Port | `4010` |
| Endpoint | `http://192.168.50.225:4010/v1` |
| Current role | Active Open WebUI OpenAI-compatible endpoint; local-first model dispatch; explicit local model selection; explicit/manual OpenRouter-free exposure |
| Target role | Single model-facing API registry for Open WebUI, OpenCode, Continue.dev, and scripts |

Documented visible model categories:

- Auto local routes: `auto-local`, `auto-coding-local`, `auto-reasoning-local`,
  `auto-small-local`.
- Explicit local models: `strix-reasoning-qwen3.6-65k`,
  `strix-coder-qwen3-coder-next-65k`, `amd-coder-qwen3-coder-30b-32k`,
  `amd-backup-gemma4-26b-8k`.
- OpenRouter-free choices: `openrouter-free/openrouter/auto-free-router` and
  `openrouter-free/<verified-model>:free` entries.

Documented behavior:

- `model-dispatch` forces `stream: false` when forwarding chat completion
  requests to local and OpenRouter-free upstreams.
- Open WebUI can keep streaming behavior at its own API boundary while
  `model-dispatch` normalizes upstream calls to non-streaming JSON.

## LiteLLM Rollback Status

LiteLLM is documented as rollback/history only.

| Field | Value |
|---|---|
| Host | `thinkcentre` |
| Path | `/srv/litellm` |
| Config | `/srv/litellm/config.yaml` |
| Compose | `/srv/litellm/docker-compose.yml` |
| Port | `4000` |
| Endpoint | `http://192.168.50.225:4000/v1` |
| Container | `litellm` |
| Documented image | `ghcr.io/berriai/litellm:main-latest` |
| Current role | OpenCode rollback and Open WebUI rollback/history unless explicitly reactivated |

Baseline risk:

- `main-latest` is documented as not ideal for a security-sensitive routing
  service. Because LiteLLM is no longer the default OpenCode path or active Open
  WebUI path, this remains a recorded rollback/history risk, not a Slice 0 fix.

## OpenRouter-Free Artifact Status

OpenRouter remains allowed only as explicit free-model access.

Documented baseline:

- OpenRouter paid catalog must remain hidden.
- OpenRouter must remain free-only and fail-closed.
- OpenCode exposes `homelab-openrouter-free` only as a manual provider.
- Open WebUI exposes `openrouter-free/openrouter/auto-free-router` and specific
  verified `openrouter-free/<verified-model>:free` entries through
  `model-dispatch`.
- 25 verified free OpenRouter models are documented.
- Generated neutral artifacts exist under `/srv/openrouter-free/`.
- The useful retained logic is free-model discovery and zero-price filtering.

Documented old LiteLLM refresh details:

- Timer: `litellm-free-models-refresh.timer`.
- Service: `litellm-free-models-refresh.service`.
- Refresh script: `/usr/local/sbin/litellm-refresh-free-models`.
- Renderer: `/srv/litellm/render-config.py`.

No timer, service, script, or generated artifact was inspected or changed during
this slice.

## Current Known Model Endpoints

| Role | Model label / target | Endpoint | Current documented use |
|---|---|---|---|
| Primary local coding | `homelab-local/Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` | `http://192.168.50.252:8083/v1` | OpenCode default coding work |
| OpenCode small model backup | `homelab-local-backup/google_gemma-4-26B-A4B-it-Q4_K_M.gguf` | `http://192.168.50.252:8084/v1` | Direct AMD RX 7900 XT backup provider |
| Strix reasoning | `Qwen3.6-35B-A3B-UD-Q4_K_XL.gguf` / `strix-reasoning-qwen3.6-65k` | `http://192.168.50.11:8081` | Advisor/planning and reasoning |
| Strix coder testbed | `Qwen3-Coder-Next-UD-Q4_K_XL.gguf` / `strix-coder-qwen3-coder-next-65k` | `http://192.168.50.11:8082` | Manual coder testbed |
| Open WebUI auto local | `auto-local`, `auto-coding-local`, `auto-reasoning-local`, `auto-small-local` | `http://192.168.50.225:4010/v1` | Local-first `model-dispatch` routes |
| Open WebUI manual free cloud | `openrouter-free/openrouter/auto-free-router`, `openrouter-free/<verified-model>:free` | `http://192.168.50.225:4010/v1` | Explicit free-only OpenRouter choices |
| LiteLLM rollback | verbose LiteLLM model IDs | `http://192.168.50.225:4000/v1` | Rollback/history and documented Continue.dev current path |

No live health checks were run for this Slice 0 baseline.

## OpenCode Current Routing Posture

Documented current OpenCode posture:

| Field | Value |
|---|---|
| Host | `amd` |
| Tool | OpenCode |
| Binary | `/home/enzo/.opencode/bin/opencode` |
| Version | `1.14.39` |
| Config path | `/home/enzo/.config/opencode/opencode.json` |
| Default provider | `homelab-local` |
| Default base URL | `http://192.168.50.252:8083/v1` |
| Default model | `homelab-local/Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` |
| Backup provider | `homelab-local-backup` |
| Backup base URL | `http://192.168.50.252:8084/v1` |
| Small model | `homelab-local-backup/google_gemma-4-26B-A4B-it-Q4_K_M.gguf` |
| Manual provider | `homelab-openrouter-free` |
| Manual provider model count | 25 verified free OpenRouter models |
| OpenRouter default usage | Disabled/manual-only |
| Automatic OpenRouter fallback | None |
| LiteLLM rollback provider | `homelab` |
| LiteLLM rollback base URL | `http://192.168.50.225:4000/v1` |

Conclusion:

- OpenCode no longer defaults through LiteLLM.
- Direct AMD routing remains the current default and also a future rollback path
  until OpenCode-through-`model-dispatch` is validated.
- OpenRouter remains manual-only.

## Continue.dev Current Routing Posture

Documented current posture:

- Continue.dev on Framework currently uses LiteLLM-routed model IDs from
  `thinkcentre:4000`.
- Continue.dev is treated as an editor-side shared routing client.
- Future roadmap slice 9 moves Continue.dev through `model-dispatch` after
  validation.

## CodeGraphContext and MCP Posture

Documented baseline:

- CodeGraphContext is optional tooling.
- Live OpenCode CodeGraphContext MCP enablement is deferred.
- Live OpenCode config remains unchanged.
- CodeGraphContext may read from approved canonical project repositories.
- CodeGraphContext must not write directly to canonical working trees by
  default.
- Any write-capable use must happen only inside a disposable sandbox, worktree,
  temporary branch checkout, dedicated patch-proposal directory, or `/tmp` patch
  artifact.
- No persistent broad mutation approval is allowed.
- No automatic MCP setup wizard may be run against primary repositories.
- No MCP tooling is installed on Cubies.

Slice 0 did not enable MCP or inspect live MCP config.

## Git Source and Mirror Posture

Documented source convention:

```text
Canonical source: strix:/srv/projects/<project-name>/
Tier-1 mirror: thinkcentre:/srv/git/<project>.git
Tier-2 mirror: mac mini:~/git-mirrors/<project>.git
```

Current documented posture:

- `strix:/srv/projects/homelab/` is the canonical homelab repo location.
- Strix is the target canonical project/source host.
- AMD remains an intentional exception for projects that need the RTX 3090
  directly and is also the current OpenCode execution host.
- ThinkCentre is the target tier-1 mirror host.
- Mac Mini is the future tier-2 mirror host.
- `cubie-camera-node` canonical source exists at
  `strix:/srv/projects/cubie-camera-node`.
- `cubie-camera-node` ThinkCentre mirror exists at
  `/srv/git/cubie-camera-node.git`.
- `cubie-camera-node` mirror default branch is `main`.
- `cubie-camera-node` initial skeleton commit is documented as `8fcd154`.
- `cubie-camera-node` hardware readiness checklist commit is documented as
  `3449eba`.

No repo locations or remotes were changed during this slice.

## Backup and Off-Site Roles

Documented baseline:

- `minipc` is the LAN backup target and target LAN backup/artifact storage host.
- `oracle` hosts Headscale and off-site backup/external anchor duties.
- `mac mini` remains Apple/iMessage services and future tier-2 git mirror.

Detailed backup/restore hardening is deferred to transition slice 12.

## Cubie/Camera Edge Node Posture

Documented baseline:

- Cubies remain edge camera nodes only.
- Cubie camera-node source repo exists on Strix.
- ThinkCentre mirror exists and tracks `main`.
- Hardware readiness checklist exists.
- No Cubie runtime state has been changed.
- Runtime changes wait for hardware readiness and explicit approval.

## Known Repo State and Untracked Paths

Read-only repo checks used:

- `git status --short`
- `rg --files`

Known untracked path at baseline:

- `tools/`

Known tracked or repo-visible inventory paths relevant to this baseline:

- `ROUTING_INVENTORY.md`
- `inventory/models/llm-runtime-topology.md`
- `inventory/mcp/codegraphcontext.md`
- `inventory/mcp/opencode-codegraphcontext-template.md`
- `inventory/mcp/opencode-codegraphcontext-live-enable.md`
- `inventory/mcp/codex-codegraphcontext-rollback.md`
- `inventory/mcp/project-registry.json`

`tools/` was not read, edited, added, removed, or otherwise touched.

## Slice 0 Risks and Open Questions

- This baseline is derived from existing documentation, not live probing. Some
  runtime state may have drifted since the prior inventories.
- LiteLLM `main-latest` remains a documented rollback/history risk.
- OpenRouter-free refresh behavior is documented from prior inventory but was
  not revalidated during this slice.
- Continue.dev still depends on LiteLLM-routed model IDs until a later validated
  migration.
- OpenCode still routes directly to AMD until a later validated
  OpenCode-through-`model-dispatch` slice.
- Backup/restore details are high-level and need slice 12 hardening.

## Next Recommended Action

Proceed to transition slice 1: prepare `model-dispatch` as a first-class repo
boundary without changing the live service.

Before any live change, produce an operator approval brief with intended change,
affected files/services, rollback path, validation commands, and the exact
command block.
