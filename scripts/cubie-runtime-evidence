#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

PYTHONPATH="${repo_root}/tools/hardware${PYTHONPATH:+:${PYTHONPATH}}" \
  exec python3 "${repo_root}/tools/hardware/cubie_runtime_evidence.py" "$@"
